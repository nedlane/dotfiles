# hosts/av/host.zsh — autonomous race-car Jetson overlay. Sourced before oh-my-zsh.sh.

# --- machine identity (consumed by shared/zsh/zshrc) ---
# peach accent (matches the tmux status-bar color); prompt shows the hostname.
export DOTFILES_ACCENT=216
export DOTFILES_ACCENT_HEX="#fab387"
